// Re-export everything from the context-based implementation for backwards compatibility
export { DEFAULT_SETTINGS, loadSettings, useMelodiqSettings as useSettings } from './SettingsContext';
export type { SettingsState } from './SettingsContext';
