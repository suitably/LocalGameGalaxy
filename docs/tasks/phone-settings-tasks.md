# Phone Settings Tasks

- [x] Create `ClientProfile` state in `PhoneClientEngine.tsx` and load/save to `localStorage`.
- [x] Update `getIdentity` in `PhoneClientEngine.tsx` to use the dynamic `ClientProfile`.
- [x] Update `mic.start()` in `PhoneClientEngine.tsx` to use the selected `micDeviceId`.
- [x] Create `ClientSettings.tsx` component with UI for Name, Hue, and Microphone Selection.
- [x] Add routing and a Settings button to the Client view in `MelodiqGame.tsx`.
- [x] Add mechanism to trigger identity update to Host when settings change.
