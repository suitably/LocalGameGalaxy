# Tasks: Helper Default Settings

- [x] Update `server/config.js` to add `defaultDownloadMode` and `autoVocalSeparation` getters/setters.
- [x] Update `server/src/routes/index.js` to add `/api/config/preferences` GET and POST endpoints.
- [x] Update `server/src/services/download.js` to trigger vocal separation on job completion if enabled.
- [x] Update `server/public/index.html` to add settings UI for Admins.
- [x] Update `server/public/index.html` download logic to use the fetched `defaultDownloadMode`.
- [x] Verify functionality via the UI.
